export enum serviceTypeEnum{
    pet,
    vet
}

export enum PetTypeEnum{
    none,
    Dog,
    Cat
}

export enum PetGenderEnum{
    Male,
    Female
}

export enum PetSizeEnum{
    Small,
    Medium,
    Large
}
